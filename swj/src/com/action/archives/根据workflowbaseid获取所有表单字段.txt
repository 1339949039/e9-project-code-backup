select id  ,               fieldname ,          b.labelname ,        viewtype,           detailtable    from (select id, fieldname, fieldlabel, viewtype, detailtable           from workflow_billfield          where billid = (select formid from workflow_base where id=$process$)                    ) a   left join htmllabelinfo b on a.fieldlabel = b.indexid                            and languageid = 7
